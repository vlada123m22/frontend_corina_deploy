# Backend configuration for Docker
# For local development with Docker Compose, use: BACKEND_HOST=backend:8081
# For production, use your actual backend URL: BACKEND_HOST=api.yourdomain.com:8081

BACKEND_HOST=backend:8081
